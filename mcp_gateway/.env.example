# Copy to .env and adjust. Run `dart_frog dev` from mcp_gateway/.

# Student REST API (Dart Frog backend), no trailing slash
BACKEND_BASE_URL=http://localhost:8080

# Mistral (chat completions)
MISTRAL_API_KEY=
MISTRAL_MODEL=mistral-small-latest
