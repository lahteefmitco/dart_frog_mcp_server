# Copy to .env and adjust. Used by dart_frog dev from the backend/ directory.

# Option A: single URL
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/students

# Option B: discrete variables (used if DATABASE_URL is unset)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=students
DB_USER=postgres
DB_PASSWORD=postgres
